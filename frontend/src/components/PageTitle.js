import React from 'react';

function PageTitle()
{
   return(
    <h2 
      id="title"
      style={{
        fontStyle: 'italic',
        fontSize: 40,
        paddingLeft: 50
      }}
    >
       Locked In
    </h2>
   );
};

export default PageTitle;
